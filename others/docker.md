## Comando 'Rodar' o Banco de Dados:

```shell
$ docker compose up -d
```

## Comando 'Parar' o Banco de Dados:

```shell
$ docker compose stop
```

## Comando 'Parar & Deletar' o Banco de Dados:

```shell
$ docker compose down
```
